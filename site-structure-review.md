# Z.ONE 网站结构审查｜2026-09-23

## 结论

网站现在最清楚、可验证的两类内容是 **已开放的课程** 与 **可安装的设计 Skills**；最缺的是让访客直接看到 Z.ONE 已做出的作品。首页多次解释同一组三个入口，反而延迟了看到成果。建议将网站压成“主张 → 真实作品/方法案例 → 明确去向 → 联系”。

此次是结构审查，尚未按下表删除既有页面或业务入口。Skill 封面中的虚构用途图另按用户反馈改为实际演示或诚实的无案例状态。

| 处理 | 对象 | 依据与建议 |
| --- | --- | --- |
| 保留 | 首页“把灵感做成作品” | `index.html` 的 Hero 主张能概括方向；缩短后续解释，让作品紧跟主张。 |
| 保留 | 两门已开放课程 | `school/index.html` 的 Eazo、Codex 课程各有真实入口；保持直达。 |
| 保留 | 设计 Skills | `library/skills/` 已能按任务筛选、查看来源、复制安装与用法；真实流程演示可继续扩充。 |
| 保留但明确身份 | 粒子互动 | 可作为 Z.ONE 的互动作品/技术案例；不要让四幕滚动故事代替作品信息。 |
| 下线 | 本机“账号/20 积分” | `index.html` 的积分只在本机保存，`app.js` 没有消费与账户同步；容易使人误以为已有可使用的会员权益。 |
| 删除占位卡 | “正在发生”中的两张无链接记录 | `index.html` 的黑客松和分享卡只写“整理中/持续更新”，访客无法查看活动成果。完成真实案例后再上。 |
| 合并 | 首页多次三入口分流 | 顶部导航、Hero 三步、关于三柱、三门卡、入口弹窗重复说明相近目的。保留一次直接可点的导航，正文解释一次即可。 |
| 降级或改写 | 造物门诊“预诊” | `clinic/index.html` 接收截图/录屏，但 `app.js` 没有读取画面，只按输入文本拼出三个通用问题。移出主导航或改成真实的咨询说明与联系入口；实际分析能力就绪后恢复。 |
| 合并 | `/library/` 到 `/library/skills/` 的中转 | 自有 Skills 在第三级，六个外部参考站却占主页面。首页与导航直接进 Skill 画廊；原 `/library/` 可保留兼容入口或收成“外部参考”。 |
| 收起 | 规划中课程与 Vibe Motion 长段示意 | `school/index.html` 的中/高阶没有课程内容；Vibe Motion 是方法预演。留一张清楚标注状态的短卡，待实际成品出现再放大。 |
| 待确认 | 首页大篇幅赞助区 | `index.html` 在作品证据之前就展开赞助。若当前主要目标并非招募赞助，先缩成页脚合作联系。 |

## 建议的导航与页面职责

1. **首页**：一句主张，前段放 2–3 个可打开的真实作品或方法案例，每个写清用途、做了什么、当前状态；随后进入 Skills、课程和联系。
2. **作品/案例**：客户项目、原创互动与虚构流程演示分开标记；视觉预览、可打开结果、职责和证据状态一致。
3. **设计 Skills**：直接进入 `library/skills/`，保留来源、用法和案例；没有可公开案例时不造图。
4. **AI 造物社**：只突出已开放课程，规划中的内容收进短更新。
5. **造物门诊**：保留服务方向，但必须让网页承诺与实际可完成的动作一致。

## 实施时的技术边界

- `library/skills/index.html` 由 `scripts/build-skills.mjs` 生成；修改卡片应先改生成器。
- `scripts/build-static.mjs` 会公开复制整个 `library/`；案例的历史稿、源码和活动数据都应确认可公开。本次案例全部为虚构活动，无私有资料。
- 现有 `scripts/smoke.cjs` 对积分、入口弹窗、空课程标签和资源中转有固定断言；实际精简时要同步调整测试。
- `assets/particle/three.core.min.js` 虽没有页面直接引用，但由 `three.module.min.js` 间接导入，不能当作无用文件删除。
